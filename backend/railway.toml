[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && daphne -b 0.0.0.0 -p $PORT backend.asgi:application"
healthcheckPath = "/api/token/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
